---
name: "Memory Search"
description: "Интеллектуальный поиск по заметкам в agency-memory с фильтрами по типу, тегам, дате и содержимому"
alwaysAllow: ["Grep", "Glob", "Read", "Bash"]
---

# Memory Search - Поиск в Agency Memory

Мощный поиск по долгосрочной памяти с множеством фильтров и умными подсказками.

## Режимы поиска

### 1. Быстрый поиск (по содержимому)
```
/memory-search "текст для поиска"
```
Ищет текст во всех заметках.

### 2. Поиск по фильтрам
```
/memory-search --type=insight --tag=frontend
/memory-search --project=2penguins --date=2026-02
/memory-search --author=craft-agents
```

### 3. Комбинированный поиск
```
/memory-search "React performance" --type=insight --tag=frontend
```

## Поддерживаемые фильтры

### Фильтр: type
```
--type=insight           # Только insights
--type=decision          # Только decisions
--type=best-practice     # Только best practices
--type=playbook          # Только playbooks
```

### Фильтр: tag
```
--tag=frontend           # Заметки с тегом frontend
--tag=react,performance  # Заметки с любым из тегов
```

### Фильтр: project
```
--project=2penguins      # Заметки проекта 2penguins
--project=ws-agency      # Заметки проекта ws-agency
```

### Фильтр: date
```
--date=2026-02           # Заметки за февраль 2026
--date=2026-02-15        # Заметки за конкретный день
--date=2026              # Заметки за весь 2026 год
```

### Фильтр: author
```
--author=craft-agents    # Заметки от Craft Agents
--author=claude-code     # Заметки от Claude Code
--author=human           # Ручные заметки
```

### Фильтр: status
```
--status=active          # Активные заметки
--status=archived        # Архивные заметки
--status=draft           # Черновики
```

## Процесс поиска

### Шаг 1: Парсинг запроса

Определи:
1. **Текст поиска** - что искать в содержимом
2. **Фильтры** - по каким критериям фильтровать
3. **Сортировка** - как упорядочить результаты

Пример:
```
Запрос: /memory-search "API design" --type=decision --project=2penguins

Парсинг:
  Текст: "API design"
  Фильтры: type=decision, project=2penguins
  Сортировка: по дате (новые первые)
```

### Шаг 2: Поиск файлов

**Сначала найди все файлы заметок:**
```
# Используй Glob tool (быстрее и кроссплатформенно)
Glob(pattern="**/*.md", path="D:\Claude\agency-memory")

# Затем фильтруй системные файлы:
# - Исключи README.md
# - Исключи memory-export-*.md (это экспорты)
```

**Фильтруй по типу (если указан):**
- `--type=insight` → только файлы в `memory-exports/insights/`
- `--type=decision` → только файлы в `memory-exports/decisions/`
- И т.д.

### Шаг 3: Чтение YAML frontmatter

Для каждого файла:
1. Прочитай первые 20 строк (frontmatter)
2. Извлеки метаданные: `type`, `tags`, `project`, `date`, `author`, `status`
3. Примени фильтры

**Оптимизация:**
- НЕ читай весь файл, если фильтры по frontmatter не прошли
- Используй Grep для быстрого поиска в frontmatter

### Шаг 4: Поиск в содержимом

Если есть текстовый запрос:
```bash
grep -r "API design" memory-exports/ playbooks/ templates/ \
  --include="*.md" \
  -n \
  -i \
  -C 2
```

Параметры:
- `-r` - рекурсивно
- `-n` - показывать номера строк
- `-i` - игнорировать регистр
- `-C 2` - показывать 2 строки контекста

### Шаг 5: Форматирование результатов

Выведи результаты в таблицу:

```datatable
{
  "title": "Результаты поиска: API design",
  "columns": [
    { "key": "type", "label": "Тип", "type": "badge" },
    { "key": "title", "label": "Заголовок", "type": "text" },
    { "key": "project", "label": "Проект", "type": "text" },
    { "key": "tags", "label": "Теги", "type": "text" },
    { "key": "date", "label": "Дата", "type": "date" },
    { "key": "file", "label": "Файл", "type": "text" }
  ],
  "rows": [
    {
      "type": "decision",
      "title": "REST vs GraphQL API",
      "project": "2penguins",
      "tags": "backend, api, graphql",
      "date": "2026-02-10",
      "file": "decision-rest-vs-graphql.md"
    }
  ]
}
```

**Сделай заголовок кликабельным:**
Оберни в markdown link к файлу:
```markdown
[REST vs GraphQL API](D:\Claude\agency-memory/memory-exports/decisions/decision-rest-vs-graphql.md)
```

### Шаг 6: Показать превью контента

Для каждого результата покажи краткое превью:

```
📄 **decision-rest-vs-graphql.md** (2026-02-10)
   Проект: 2penguins | Теги: backend, api, graphql

   > ## Контекст
   > Нужно выбрать подход к **API design** для нового сервиса...

   [Открыть полностью](D:\Claude\agency-memory/...)
```

## Умные подсказки

### Автодополнение тегов

Если пользователь начал вводить тег:
```
/memory-search --tag=fro
```

Предложи варианты:
```
Возможно, вы имели в виду:
  --tag=frontend
  --tag=frontend-optimization
```

### Похожие заметки

Если найдено мало результатов, предложи похожие:

```
Найдено: 1 результат

Возможно, вас также заинтересует:
  - decision-api-versioning.md (похожие теги: backend, api)
  - insight-graphql-n-plus-one.md (похожий проект: 2penguins)
```

### Альтернативные запросы

Если ничего не найдено:

```
❌ Результатов не найдено

Попробуйте:
  - Убрать фильтры (--type, --tag)
  - Использовать другие ключевые слова
  - Проверить орфографию

Или создать новую заметку: /memory-write
```

## Продвинутые возможности

### 1. Поиск по регулярному выражению

```
/memory-search --regex "API.*design" --type=decision
```

Использует Grep с регулярными выражениями.

### 2. Поиск с исключениями

```
/memory-search "React" --exclude-tag=deprecated --exclude-project=old-project
```

### 3. Ранжирование результатов

Сортируй результаты по релевантности:

**Приоритет 1:** Совпадение в заголовке
**Приоритет 2:** Совпадение в tags
**Приоритет 3:** Совпадение в содержимом
**Приоритет 4:** Дата (новые первые)

### 4. Экспорт результатов

```
/memory-search "React" --export=search-results.md
```

Создает файл с найденными заметками и ссылками на них.

## Статистика поиска

После вывода результатов покажи статистику:

```
📊 Статистика поиска

Всего заметок: 47
Найдено: 8
Типы:
  - insight: 5
  - decision: 2
  - best-practice: 1
Проекты:
  - 2penguins: 4
  - ws-agency: 3
  - wk-connect: 1
Период: 2026-01-15 — 2026-02-15
```

## Интерактивный режим

Если пользователь вызвал `/memory-search` без параметров:

```
🔍 Интерактивный поиск в Agency Memory

Что ищем?
  [1] Текстовый поиск
  [2] Поиск по фильтрам
  [3] Просмотр всех заметок

Выберите режим [1-3]:
```

### Режим 1: Текстовый поиск
```
Введите текст для поиска:
> React performance

Искать в:
  [1] Всех заметках
  [2] Только insights
  [3] Только decisions
```

### Режим 2: Поиск по фильтрам
```
Фильтр по типу [Enter для пропуска]:
> insight

Фильтр по тегам [Enter для пропуска]:
> frontend, react

Фильтр по проекту [Enter для пропуска]:
> 2penguins

Фильтр по дате [Enter для пропуска]:
> 2026-02
```

### Режим 3: Просмотр всех заметок
```
Показать все заметки? Это может занять время.

Сортировка:
  [1] По дате (новые первые)
  [2] По типу
  [3] По проекту
```

## Кэширование результатов

Для ускорения повторных поисков:

1. **Создай индекс** (опционально):
   ```
   # При первом запуске используй Glob
   Glob(pattern="**/*.md", path="D:\Claude\agency-memory")
   # Сохрани результаты для последующего использования
   ```

2. **Используй индекс**:
   Читай из `.search-index.txt` вместо повторного `find`

3. **Обновляй индекс**:
   После `/memory-write` или `/memory-sync` обнови индекс

## Что НЕ делать

❌ НЕ ищи в `.git` папке
❌ НЕ ищи в binary файлах
❌ НЕ показывай более 50 результатов без предупреждения
❌ НЕ читай весь файл, если нужен только frontmatter
❌ НЕ забывай про case-insensitive поиск

## После поиска

Предложи пользователю:
1. Открыть найденную заметку
2. Создать новую заметку на основе результатов
3. Экспортировать результаты
4. Уточнить поиск
